# Circuit Description

The DHT11 sensor is connected to the ESP32 microcontroller.

- VCC → 3.3V
- GND → GND
- Data Pin → GPIO (configured in code)

The ESP32 reads sensor values and processes them for output.
